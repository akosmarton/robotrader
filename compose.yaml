services:
  app:
    image: docker.io/makos/robotrader
    build:
      context: .
    environment:
      STORAGE_DIR: /data
      ALPACA_API_KEY:
      ALPACA_API_SECRET:
      TELEGRAM_BOT_TOKEN:
      TELEGRAM_CHAT_ID:

